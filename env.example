# Email Service Configuration for Taklaget
#
# This app uses an Email Center abstraction.
# Configure your SMTP connection via Firebase Console Extensions (not .env)
# or enable SendGrid later via functions configuration.
#
# Email Center mode:
# - disabled: no emails sent, only logged
# - log-only: log requests without sending
# - enabled: send via selected provider
VITE_EMAIL_SERVICE_MODE=disabled
# Provider registry (for future expansion): trigger-email-extension | sendgrid
VITE_EMAIL_PROVIDER=trigger-email-extension
# Optional reason shown in logs
VITE_EMAIL_SERVICE_DISABLED_REASON=Email service is disabled in this environment

# Only set the brand "from" address for frontend emails (if needed):
VITE_FROM_EMAIL=noreply@taklaget.app

# Firebase Configuration
# Required for production. Optional for local dev (falls back to hardcoded values).
# Get values from Firebase Console > Project Settings > Your apps
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_auth_domain
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_storage_bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
