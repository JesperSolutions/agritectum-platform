// Offer lifecycle flow regression tests.
// Scaffolded by Phase 0 of the user-flow audit plan.
import { describe, it } from 'vitest';

describe('offer lifecycle', () => {
  describe('OffersPage delete', () => {
    it.todo('requires AccessibleModal confirmation before deleting an offer');
  });

  describe('follow-up automation', () => {
    it.todo('triggers follow-up email when offer is sent and not responded to');
  });
});
